# Core simulation engine
pandas>=2.2.0
openpyxl>=3.1.2

# MILP exact solver (bundles CBC binary)
pulp>=3.0.0

# UI
streamlit>=1.32.0
plotly>=5.20.0
# kaleido — Plotly's static image renderer. Used to embed the 3D iso as
# a PNG for browser Cmd+P printing (Chrome/Safari print WebGL canvases
# as blank, so we ship a server-side rendered fallback).
kaleido>=0.2.1

# Excel generation (PDF removed 2026-05-19 — HTML print via Cmd+P only)
xlsxwriter>=3.2.0

# Phase 1 (uncomment when ready):
# fastapi>=0.110.0
# uvicorn[standard]>=0.27.0
# sqlalchemy>=2.0.27
# psycopg2-binary>=2.9.9
# alembic>=1.13.1
# python-multipart>=0.0.9
# python-jose[cryptography]>=3.3.0
# python-saml>=1.16.0  # for SSO
# pydantic>=2.6.0
# pydantic-settings>=2.2.0

# Dev/test
pytest>=8.0.0
pytest-cov>=4.1.0
